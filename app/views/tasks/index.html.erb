<h1>All the tasks 🚀</h1>
<ul>
  <% @tasks.each do |task| %>
    <li>the task is <%= link_to task.title, task_path(task) %> </li>
  <% end %>
</ul>

<p> <%= link_to "create a new task", tasks_new_path %> </p>
